{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  },
  "exclude": [
    "node_modules",
    "tests",
    "playwright_*.ts",
    "verify_*.spec.ts",
    "**/*.spec.ts"
  ]
}
